# Hermes Config & Customization Repository

Local config management and auto-sync repository for Hermes Agent.

## Setup Instructions
1. Authenticate with GitHub using SSH:
   * Copy the public SSH key to GitHub Settings.
2. Initialize and pull latest settings:
   * `./scripts/sync-config.sh`
